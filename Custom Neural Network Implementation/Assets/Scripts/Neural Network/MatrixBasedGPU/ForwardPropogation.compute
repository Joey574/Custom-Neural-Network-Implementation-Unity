#pragma kernel CSMain

int count;
int dispatchSize;
Buffer<float> input;

RWBuffer<float> total;
RWBuffer<float> activation;

float ReLU(float x)
{
    return max(0.0f, x);
}

float Sum()
{
    
}

[numthreads(8,8,1)]
void CSMain (uint3 id : SV_DispatchThreadID)
{
    for (int i = id.x + (id.y * dispatchSize); i < count; i++)
    {
        
    }
}